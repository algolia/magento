[build]
    base    = ""
    publish = "_site"
    command = "npm install; bundle exec jekyll build"

[context.production.environment]
    JEKYLL_ENV = "production"
    ROOT_PATH = "/magento/"

[context.deploy-preview.environment]
    JEKYLL_ENV = "production"
    ROOT_PATH = "/magento/"

[context.branch-deploy.environment]
    JEKYLL_ENV = "production"
    ROOT_PATH = "/magento/"